{
  "name": "city-bikes",
  "version": "1.0.0",
  "scripts": {
    "start-client": "cd city-bikes-client && npm start",
    "start-server": "cd city-bikes-server && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run start-server\" \"npm run start-client\""
  },
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
